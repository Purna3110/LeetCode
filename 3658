class Solution:
    def gcdOfOddEvenSums(self, n: int) -> int:
        oddsum=0
        evensum=0
        for i in range(1,n*2+1):
            if i%2==0:
                evensum=evensum+i
            else:
                oddsum=oddsum+i
        a=math.gcd(oddsum,evensum)
        return a
